class Solution {
    int countStrings(int n) {
        int zeros = 1, ones = 1;

        for (int i = 2; i <= n; i++) {
            int newZeros = zeros + ones;
            int newOnes  = zeros;
            zeros = newZeros;
            ones  = newOnes;
        }

        return zeros + ones;
    }
}

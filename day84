class Solution {
    public int findSmallest(int[] arr) {
        Arrays.sort(arr);
        int result = 1;
        
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] > result) {
                return result;
            }
            result += arr[i];
        }
        
        return result;
    }
}
